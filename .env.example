# Copy to `.env` for local dev or set in your CI/host environment.

# ITPapp web app / login URL
VITE_APP_URL=https://quality.ozcc.com.au

# Contact form API endpoint (the ContactApiUrl output from the CDK stack)
VITE_CONTACT_API_URL=
